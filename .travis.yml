language: node_js
node_js:
  - "8.11.2"
cache:
  yarn: true
  directories:
  - node_modules
script:
- yarn build
- yarn lint
notifications:
  email: false
  slack: toysmore:r1Bbi4nviv7a4NCtr2dWUUig
    on_success: change
    on_pull_requests: false
    template:
      - "%{repository_slug} (%{commit}) : %{message}"
      - "Build details: %{build_url}"
